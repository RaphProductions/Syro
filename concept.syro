const syro = @include("/syro");

var number: i32 = 14;
const text: str = "Hello";

fn add(x: i32, y: i32): i32 {
    return x + y;
}

fn main(): void {
    var casted_number: i64 = @cast<i64>(number);
}